# Material bruto de fotografía (no se publica)
*.ARW
*.CR2
*.MOV
*.psd
.DS_Store
# Material bruto: img/ se versiona solo con archivos curados ya rastreados
# (los nuevos exports finales se agregan con: git add -f img/...)
img/*
.wrangler/
